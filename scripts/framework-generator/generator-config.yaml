# Framework generator input — the only file you need to edit.
#
#   npm run generate --prefix scripts/framework-generator -- [path/to/this-file.yaml] [--dry-run]
#
# Run from the repo root so analysisDir and outputDir resolve correctly. The app test
# base URL comes from root app-config.yaml. The generator reads the reports the repo
# analyzer writes from a local clone of the app under test and emits a Playwright
# component-object-model framework in the language named below.

# Only `typescript` renders page objects today. The other adapters emit a project scaffold and
# stop — implementing `renderPage` in `languages/<id>.ts` is what turns one into a real target.
language: typescript    # typescript | javascript | java | python | csharp
projectName: e2e

# Where the framework is written. Defaults to `generated-framework/<appName>/` — the app
# folder named in root app-config.yaml — so generating for a second app adds a folder
# instead of overwriting the first one's framework. Set it only to write somewhere else.
# outputDir: ./generated-framework/<appName>

# Where the repo analyzer's reports are read from. Defaults to `analysis/<appName>/` —
# the app folder named in root app-config.yaml — so this file stays app-agnostic. Set it
# only to read an analysis from somewhere else.
# analysisDir: analysis/<appName>

# Optional. Static analysis describes screens, never flows, so the login sequence is not
# something it can produce — point this at the app config to get a working login helper
# instead of a stub. Credentials are NOT read from it — they come from
# APP_USERNAME / APP_PASSWORD in the generated project's .env.
#
# Commented out because no login config is required for static analysis. Fill a login
# config file in, then uncomment this to generate a working login helper.
# loginConfig: app-config.yaml

pages:
  folderSegment: auto         # URL segment used as the folder group; `auto` strips the
                              # prefix every mapped URL shares, or set a 1-based number
  mergeDuplicates: true       # routes rendering the same component are one screen: the
                              # shortest URL is the page, every other mount an alias

# The application's shared navigation, declared rather than derived.
#
# This is the one part of a rendered page static analysis may not be able to reach. An app whose
# navigation is rendered by an external design-system package, or filled from a server menu
# payload, has a sidebar that appears in no template in the app's own source. Each entry is the same
# `{ name, component, locator }` shape an extracted element has, and every entry is lifted
# into a single NavigationBar component instead of being repeated on every page object.
#
# Leave the list empty for an app whose navigation is in its own markup — the extractor will
# find it, and no NavigationBar is emitted.
navigation: []
#
# A worked example, from an app whose sidebar comes from a server-rendered menu:
#
#   navigation:
#     - name: adminMenuLink
#       component: link
#       locator: {strategy: getByRole, args: ["link"], name: "Admin"}
#     - name: searchMenuInput
#       component: input
#       locator: {strategy: css, args: [".main-menu-search input"]}

waits:
  # Selector for the app's loading indicator, used by waitForSpinnerToClear().
  # The default is role-based and works on any accessible app; add your app's own
  # class if it renders a spinner with no busy state, e.g. '.my-spinner, [role="progressbar"]'.
  spinnerSelector: '[role="progressbar"], [aria-busy="true"]'

tests:
  generateSmokeSpecs: true    # one navigate + assert spec per generated page object

# Label-parameterised locator templates: the app's answer to "how do I find the input
# belonging to this label". `{label}` is substituted with the element's label, and a page
# object then reads `InputComponent.byLabel(this.page, 'City')` instead of carrying the
# selector. This block and the `navigation:` list above are the only two places an
# app-specific selector appears — retargeting means replacing them.
#
# These ids are rung 4 on the locator ladder: a template expands to CSS but is anchored to
# a label, so it breaks only when the label does. An element is emitted through a factory
# only when the template reproduces its locator exactly; anything else keeps its own, so a
# one-off never gets bent to fit. Remove a line to turn its factory off; remove the block
# for an app whose labels are properly associated (`getByLabel` is rung 3 and needs none).
# mco's design system stamps `data-e2e-label` on every labelled control, but the `data-e2e-id`
# half is per-*wrapper-tag*, not per-kind: five different wrapper tags all collapse to kind
# `input` (textinput, search-input, datepicker, date-time-picker, mco-input-spinner) and each
# stamps a different `data-e2e-id`. Pinning a pattern to one of those ids would match only the
# wrapper that happens to use it and silently miss the rest — `data-e2e-label` alone is the
# per-instance half and is what actually discriminates one control from the next, so every
# pattern below is label-only. Every id `KIND_TEMPLATES` (repo-analyzer/elements.ts) maps is
# listed here, `labelledRadio` included, since the two radio-group wrappers (`two-option-button`,
# `button-group`) depend on it — an id missing from this block is an id `templatesFrom` drops,
# and its elements fall to a weaker signal or into `skipped` instead of erroring.
locatorTemplates:
  labelledInput:     '[data-e2e-label="{label}"]'
  labelledTextarea:  '[data-e2e-label="{label}"]'
  labelledSelect:    '[data-e2e-label="{label}"]'
  labelledRadio:     '[data-e2e-label="{label}"]'
  labelledCheckbox:  '[data-e2e-label="{label}"]'
  labelledSwitch:    '[data-e2e-label="{label}"]'
  labelledButton:    '[data-e2e-label="{label}"]'
  tableByColumn:     '[data-e2e-label="{label}"]'
  topNavTab:         '[data-e2e-label="{label}"]'
#
# Every pattern above is the identical `[data-e2e-label="{label}"]` string, and that is
# deliberate, not an oversight to collapse: the per-kind `data-e2e-id` half carries no
# selector-level discrimination here, since five different wrapper tags (textinput, search-input,
# datepicker, date-time-picker, mco-input-spinner) all collapse to kind `input` while each stamps
# a *different* `data-e2e-id`. Pairing an id into the pattern would be worse than this, not better
# — it would match only the one wrapper that happens to use that id and silently miss the other
# four sharing the kind. `data-e2e-label` alone is the per-instance half, so a label-only anchor
# is the only pattern that covers every wrapper a kind maps to.
#
# That anchor is also why `check-analysis` reports roughly 1031 locator-collision warnings on the
# verification clone: a label-only selector cannot prove page-wide uniqueness the way an
# id-pinned one could. That count is the expected, honest cost of this trade-off, not a defect to
# chase to zero — pinning a specific collision down means recording the flow and adding a scoped
# accessor in the protected page object, per `locator-ladder.ts`'s rung-4 note.
#
# Leaving the block empty remains a supported state for a fresh app whose ids are not yet known:
# the extractor only proposes a template id defined here, so an empty block sends elements that
# would have reached rung 4 to a weaker signal or into `skipped` instead of erroring. Filling it
# in — as this block now does — is an improvement, never a precondition.

# Where scripts/framework-generator/smart-api-map.ts writes the normalized api-map
# (one YAML file per resource). Absent or empty directory -> no API layer is
# generated; nothing else changes.
# Defaults to `<analysisDir>/api-map`.
# apiMapDir: analysis/<appName>/api-map

api:
  enabled: true
  include: {}                 # { tags: [Users, Claims] } or { operationIds: [...] } — empty means "all"
  exclude: {}                 # same shape, subtracted after include
  generateAssertionSpecs: true  # one status/schema check per api-mapped operation
  generateFactories: true       # scaffold src/data/factories/<resource>-factory.ts stubs
