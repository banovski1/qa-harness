{
  "app": "espocrm-demo",
  "repoPath": "~/Projects/espocrm",
  "baseUrl": "https://demo.eu.espocrm.com/",
  "repoCommit": "8b4f900085d2e21097b72c8cc27e14cb1de5cbd8",
  "generatedAt": "2026-09-12T21:14:37+00:00",
  "frontend": {
    "root": "client",
    "framework": "backbone",
    "version": "1.3 (via bullbone 1.5.5 fork)",
    "router": "bullbone Router (hash)",
    "componentExt": ".js",
    "templateExt": ".tpl",
    "entry": "client/src/app.js"
  },
  "backend": {
    "root": "application/Espo",
    "framework": "php (bespoke, Slim-like DI container)",
    "version": "EspoCRM 10.0.7"
  },
  "testId": {
    "attribute": null,
    "occurrences": 0,
    "note": "no data-testid/test/cy/qa anywhere in client/. The app does carry data-name on 215 source files, which is a stable per-field marker and the best available substitute \u2014 the crawl should treat it as a scoping attribute, not as a test id."
  },
  "mountPrefix": "/",
  "apiDocs": {
    "kind": "none",
    "path": null,
    "note": "no OpenAPI document in the repo; the REST surface is declared by PHP controllers (Tier B)"
  },
  "notes": [
    "Routes are generic patterns, not a route table. client/src/router.js:104 declares ':controller', ':controller/create', ':controller/view/:id' and so on; the concrete screen list is those patterns expanded over the entity scopes in application/Espo/Resources/metadata/scopes/*.json where entity and object are both true (14 entities).",
    "No route names a view component. A route resolves to a controller which picks its view at runtime from clientDefs metadata, so every route's component is null by design. Elements for these screens can only come from the crawl.",
    "'*actions' (router.js:148) is the fallback handler, not a screen, and is skipped."
  ]
}
