{
  "app": "calcom",
  "repoPath": "~/Projects/cal.diy",
  "baseUrl": "https://cal.com/",
  "repoCommit": "b0a34f21c91ae7803f9b7e2c59c2fbd187cce26f",
  "generatedAt": "2026-09-12T21:46:09+00:00",
  "frontend": {
    "root": "apps/web",
    "framework": "next.js",
    "version": "app-router",
    "router": "file-system (apps/web/app/**/page.tsx)",
    "componentExt": ".tsx",
    "templateExt": ".tsx",
    "entry": "apps/web/app/layout.tsx"
  },
  "backend": {
    "root": "packages/trpc",
    "framework": "tRPC over Next.js route handlers",
    "note": "there is also a REST API under apps/api, versioned separately"
  },
  "testId": {
    "attribute": "data-testid",
    "occurrences": 1176,
    "note": "the only corpus app that marks its own UI for testing. Prefer data-testid over every other strategy here."
  },
  "mountPrefix": "/",
  "apiDocs": {
    "kind": "annotations",
    "path": "packages/trpc/server/routers/viewer/*",
    "note": "tRPC procedures with Zod input schemas \u2014 the payload shapes are readable directly."
  },
  "notes": [
    "78 routes are declared. Only the public booker is reachable without an account, so the crawl covers 2 of them and every other route lands in app-model.json as crawled:false. That asymmetry is why this app is in the corpus.",
    "Route groups in parentheses \u2014 (booking-page-wrapper), (use-page-wrapper) \u2014 organise files and do not appear in the URL.",
    "An optional catch-all segment ([[...step]]) resolves to the URL without it, the same rule an optional group follows in a hash router."
  ]
}
