{
  "app": "odoo",
  "repoPath": "~/Projects/odoo",
  "baseUrl": "http://localhost:8069/",
  "repoCommit": "f8496b42a96f35affb6bb9ecac2310e8c174d3e2",
  "generatedAt": "2026-09-12T21:55:36+00:00",
  "frontend": {
    "root": "addons/web/static/src",
    "framework": "owl",
    "version": "2.x (Odoo's own component framework)",
    "router": "none \u2014 the back office is one URL driven by actions",
    "componentExt": ".js",
    "templateExt": ".xml",
    "entry": "addons/web/static/src/main.js"
  },
  "backend": {
    "root": "odoo",
    "framework": "odoo (python, werkzeug routing via @http.route)",
    "version": "master"
  },
  "testId": {
    "attribute": null,
    "occurrences": 0,
    "note": "no data-testid in addons/web/static/src. Odoo's own tour tests address elements by class and by t-name; a crawl is the only way to get proved locators here."
  },
  "mountPrefix": "/",
  "apiDocs": {
    "kind": "annotations",
    "path": "@http.route decorators across addons/*/controllers/",
    "note": "786 routes carry an explicit decorator naming auth and type. The JSON-RPC surface (/web/dataset/*, /jsonrpc) is where all record CRUD happens."
  },
  "notes": [
    "THE IMPORTANT ONE: Odoo's back office has no per-screen URL. Everything after /odoo (or /web) is one Owl application whose screen is chosen by an *action* \u2014 a database record \u2014 and reflected in the URL fragment as an action id, not a route. So the 585 routes below are the website and portal side, plus entry points; the back office screens a QA engineer works with cannot be enumerated from source at all. Only a crawl of a running instance can list them, and this profile has no baseUrl yet.",
    "638 addons each contribute controllers. The routes here are the union across all of them, which is not what any single deployment serves: an addon that is not installed serves nothing.",
    "A route's `auth` is recorded ('public', 'user', 'none') because it decides whether a crawl can reach it at all."
  ]
}
