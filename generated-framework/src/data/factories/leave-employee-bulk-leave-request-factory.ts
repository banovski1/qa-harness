import type { APIResponse } from '@playwright/test';
import type { LeaveEmployeeBulkLeaveRequestClient } from '../../api/clients/LeaveEmployeeBulkLeaveRequestClient';

/**
 * Precondition/setup helpers for Leave/Employee Bulk Leave Request, backed by the typed API client
 * instead of the browser. Fill in real field values per creator below — the
 * api-map only knows the request shape, not what makes a valid record here.
 * Written once by the generator; never overwritten.
 */

// Leave/Employee Bulk Leave Request has no POST operation in the api-map yet — nothing to scaffold.
