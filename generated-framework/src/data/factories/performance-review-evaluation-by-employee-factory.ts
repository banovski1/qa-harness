import type { APIResponse } from '@playwright/test';
import type { PerformanceReviewEvaluationByEmployeeClient } from '../../api/clients/PerformanceReviewEvaluationByEmployeeClient';

/**
 * Precondition/setup helpers for Performance/Review Evaluation by Employee, backed by the typed API client
 * instead of the browser. Fill in real field values per creator below — the
 * api-map only knows the request shape, not what makes a valid record here.
 * Written once by the generator; never overwritten.
 */

// Performance/Review Evaluation by Employee has no POST operation in the api-map yet — nothing to scaffold.
