import { ConfigurePimPageGenerated } from './ConfigurePimPage.generated';

/**
 * ConfigurePimPage — put page-specific actions and assertions here.
 *
 * The generator created this file once and will never overwrite it. Mapped
 * elements live in ConfigurePimPage.generated.ts, which is regenerated on every run.
 */
export class ConfigurePimPage extends ConfigurePimPageGenerated {}
