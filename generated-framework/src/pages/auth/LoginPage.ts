import { LoginPageGenerated } from './LoginPage.generated';

/**
 * LoginPage — put page-specific actions and assertions here.
 *
 * The generator created this file once and will never overwrite it. Mapped
 * elements live in LoginPage.generated.ts, which is regenerated on every run.
 */
export class LoginPage extends LoginPageGenerated {}
