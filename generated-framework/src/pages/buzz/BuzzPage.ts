import { BuzzPageGenerated } from './BuzzPage.generated';

/**
 * BuzzPage — put page-specific actions and assertions here.
 *
 * The generator created this file once and will never overwrite it. Mapped
 * elements live in BuzzPage.generated.ts, which is regenerated on every run.
 */
export class BuzzPage extends BuzzPageGenerated {}
