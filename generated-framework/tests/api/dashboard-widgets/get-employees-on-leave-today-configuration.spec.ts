// AUTO-GENERATED by framework-generator. Do not edit — every run overwrites this file.
// Add real scenarios in sibling spec files; those are yours to keep.

import { test, expect } from '../../../src/fixtures';
import { assertShape } from '../../../src/utils/schema-assert';

test.describe('Dashboard/Widgets — get-employees-on-leave-today-configuration', () => {
  test('GET /api/v2/dashboard/config/employee-on-leave-today responds 200', async ({ dashboardWidgetsApi }) => {
    const response = await dashboardWidgetsApi.getEmployeesOnLeaveTodayConfiguration();
    expect(response.status()).toBe(200);
    const body = await response.json();
    assertShape(body, {"kind":"object","properties":[{"name":"showOnlyAccessibleEmployeesOnLeaveToday","type":"boolean","required":false,"nullable":false}]});
  });
});
