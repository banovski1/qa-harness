import { RelatedPageGenerated } from './RelatedPage.generated';

/**
 * RelatedPage — put page-specific actions and assertions here.
 *
 * The generator created this file once and will never overwrite it. Mapped
 * elements live in RelatedPage.generated.ts, which is regenerated on every run.
 */
export class RelatedPage extends RelatedPageGenerated {}
