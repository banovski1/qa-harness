import { CreatePageGenerated } from './CreatePage.generated';

/**
 * CreatePage — put page-specific actions and assertions here.
 *
 * The generator created this file once and will never overwrite it. Mapped
 * elements live in CreatePage.generated.ts, which is regenerated on every run.
 */
export class CreatePage extends CreatePageGenerated {}
